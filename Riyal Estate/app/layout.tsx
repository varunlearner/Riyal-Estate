import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { AuthProvider } from '@/hooks/useAuth';
import { Toaster } from 'react-hot-toast';
import Navbar from '@/components/layout/Navbar';
import Footer from '@/components/layout/Footer';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Riyal Estate - India\'s Premier Property Platform | Buy, Sell, Rent Properties',
  description: 'Discover your dream property with Riyal Estate. Buy, sell, and rent residential and commercial properties across India. Verified listings, transparent pricing, and seamless transactions.',
  keywords: 'real estate, property, buy property, rent property, India, Mumbai, Delhi, Bangalore, apartments, villas, houses',
  authors: [{ name: 'Riyal Estate' }],
  openGraph: {
    title: 'Riyal Estate - India\'s Premier Property Platform',
    description: 'Discover your dream property with Riyal Estate. Buy, sell, and rent properties across India.',
    type: 'website',
    locale: 'en_IN',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>
          <div className="min-h-screen flex flex-col">
            <Navbar />
            <main className="flex-1">{children}</main>
            <Footer />
          </div>
          <Toaster
            position="top-right"
            toastOptions={{
              duration: 4000,
              style: {
                background: '#363636',
                color: '#fff',
              },
              success: {
                duration: 3000,
                iconTheme: {
                  primary: '#22c55e',
                  secondary: '#fff',
                },
              },
              error: {
                duration: 5000,
                iconTheme: {
                  primary: '#ef4444',
                  secondary: '#fff',
                },
              },
            }}
          />
        </AuthProvider>
      </body>
    </html>
  );
}
